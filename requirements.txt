# SAR-TopoGuard — Python dependencies
# ====================================
# Install PyTorch separately with the correct CUDA variant:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Core
numpy>=1.24
tqdm>=4.65
Pillow>=9.5

# GeoTIFF I/O (at least one of these)
rasterio>=1.3
tifffile>=2023.4

# Stratification (Module 2)
opencv-python-headless>=4.8

# Training visualisation (optional but recommended)
tensorboard>=2.14

# Logging & config (stdlib — listed for clarity)
# pathlib, logging, json, argparse — all built-in
